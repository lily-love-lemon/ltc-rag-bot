# LTC RAG Bot 项目汇总 · Handoff

> 生成时间: 2026-09-19（基于生产版本 **026** 刷新，替代 018 基线）
> 当前在线版本: **生产版本 026**（2026-09-19）· V5 深空 Portal + M2 省 Token 四层漏斗 + 拒答双层 + 显卡/算力 KB 文档。
> **完整现状以同包 `handoff.md` 与 `docs/` 7+ 份交付文档为准**（KB 已 207→215、6→7 文件、Embedding 切 BAAI/bge-m3、新增拒答与省 Token 埋点）。
> 接手方式: 工作区源码已含全部改动（git master / `cc41c14`，无 remote），直接 deploy 即可；GitHub clone 当前返回 502，本地无 remote。

---

## 🔗 所有关键链接

| 资源 | URL |
|---|---|
| **GitHub 仓库** | https://github.com/lily-love-lemon/ltc-rag-bot（clone 502，本地无 remote） |
| **CloudBase Portal 在线** | https://ltc-rag-bot-315461-10-1450031534.sh.run.tcloudbase.com |
| **CloudBase 控制台** | https://tcb.cloud.tencent.com/dev?envId=lily0625ai-d1gpwc1vw89141edd#/platform-run/service/detail?serverName=ltc-rag-bot |
| **Cos Bucket** | `6c69-lily0625ai-d1gpwc1vw89141edd-1450031534` (cos.ap-shanghai.myqcloud.com) |
| **FDE Skill (本地)** | `<项目根>/.agents/skills/rag-knowledge-base-skill/` |

### CloudBase 环境变量

```
Env ID:     lily0625ai-d1gpwc1vw89141edd
Service:    ltc-rag-bot
Mem/CPU:    2核 2GB（已升级，非免费版）
COS Mount:  COS bucket /  → 容器 /mnt/chroma
Keys(9个):  ZHIPUAI_API_KEY / ZHIPUAI_MODEL=glm-4-flash
            API_KEY=<API_KEY>
            FEISHU_APP_ID / FEISHU_APP_SECRET
            EMBED_BACKEND=siliconflow / EMBED_MODEL=BAAI/bge-m3 / EMBED_API_URL / EMBED_API_KEY
            + SOFT_REFUSE_THRESHOLD=0.45
```
> ⚠️ 密钥只存在于 CloudBase 控制台环境变量，**不在代码、不在 `.env` 文件、不在交付包**（打包已排除 `.env`）。改阈值/换后端后必须通过控制台或 `/embed/rebuild` 生效。

### 部署版本演进（DeployId 015 → 026）

| 版本 | 时间 | 关键内容 | 本地 git tag / commit |
|---|---|---|---|
| 015 | 09-18 | Trae 基线冒烟全绿 | `v3-20260918-smoke-green` |
| 016 | 09-18 | V4 功能上线 | `v4-deployed` |
| 017→018 | 09-18 | 紫色 hero + Portal 字段对齐 + bm25 | `v4.1-purple-hero` |
| **019** | 09-18 22:08 | 本地优化版经**连接器**首部署（规格零降级） | `opt-20260918-deploy-live-019` / `6ae6577` |
| 020 | 09-18 22:58 | #5 可插拔 Embedding 后端 | `opt-20260918-embed-pluggable` / `bb4a912` |
| 021 | 09-18 23:10 | 激活 bge-m3 + 健壮性（BM25 兜底） | `opt-20260918-embed-live` / `0bc3bd2` |
| 022 | 09-19 00:30 | **Portal V5 深空指挥台 + 文档 CRUD** | `opt-20260918-portal-v5` / `db536cc` |
| 023→024 | 09-19 01:15 | **M2 省 Token 三件套**（L1+L2+埋点） | `opt-20260919-m2-token-economy` / `7ca11cf` + `13e6993` |
| 025→026 | 09-19 02:20 | **拒答双层修复 + GPU KB 文档**（207→215） | `opt-…-refuse` / `6bebf7b` → master `cc41c14` |
| **026（当前）** | 09-19 | 以上全部在线，9 环境变量、规格零降级 | `handoff.md` Done #29–#33 |

---

## 🏗️ 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                  客户端层                                │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐ │
│  │ Web Portal  │  │ 飞书机器人   │  │ curl API       │ │
│  │ (V5 深空)   │  │ (webhook)    │  │ /health / /kb  │ │
│  └──────┬──────┘  └──────┬───────┘  └───────┬────────┘ │
└─────────┼────────────────┼───────────────────┼──────────┘
          │                │                   │
          ▼                ▼                   ▼
┌─────────────────────────────────────────────────────────┐
│  CloudBase · FastAPI (app.py 2073 行) · port 8080       │
│  ┌────────────────────────── /query 路由链 ───────────┐ │
│  │ 0. AnswerCache L0 精确缓存：归一化同问直返（0 token） │ │
│  │ 1. AnswerCache L1 语义近似：问句向量余弦≥0.92       │ │
│  │    (hit_count≥2) 直返（省 ~404 token/次）          │ │
│  │ 2. L2 拒答短路：top1<REJECT_THRESHOLD(=0关闭)→拒答  │ │
│  │ 3. 向量召回 top15（bge-m3 / TF-IDF 可插拔）         │ │
│  │ 4. BM25 + Jieba 关键词召回 top15                    │ │
│  │ 5. RRF 融合 (k=60) → top5                          │ │
│  │ 6. ★ cosine 重排 → top3（纯 numpy, 零依赖）        │ │
│  │ 7. context = top2 × 300 字                          │ │
│  │ 8. LLM (glm-4-flash) + fabe.txt 接地契约           │ │
│  │    （含 SOFT_REFUSE_THRESHOLD=0.45 软拒答注入）      │ │
│  │ 9. AnswerCache.store + TokenEconomy 埋点            │ │
│  └────────────────────────────────────────────────────┘ │
│  ┌───────────────────────┼────────────────────────────┐ │
│  │  SimpleVectorStore  ▼  (自实现, numpy + JSON)      │ │
│  │  path = /mnt/chroma  (COS 挂载)                    │ │
│  │  ├─ metadata.json   (215 条, 含 created_at 等字段) │ │
│  │  ├─ vectors.npy      (bge-m3, 1024 维, 828KB)      │ │
│  │  ├─ vocab.json       (TF-IDF 词表, 36KB, tfidf后端)│ │
│  │  └─ answer_cache.json (L0/L1 缓存快照 + 指纹)      │ │
│  └──────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
          ▲              ▲
          │ COS Mount    │ COS Mount
          ▼              ▼
┌──────────────────────────────────────────────────────────┐
│  COS Bucket: 6c69-lily0625ai-d1gpwc1vw89141edd-1450031534 │
│  Endpoint: cos.ap-shanghai.myqcloud.com                   │
│  内容: metadata.json / vectors.npy / vocab.json / xiaoliu-identity/ │
└──────────────────────────────────────────────────────────┘
```

### 数据流 · 切片入库

```
文件 / 粘贴文本
       │
       ▼
ingest-file / ingest-text 端点
       │
       ├─ 文本分块（默认 500 char, 无重叠）
       ├─ 自动注入 metadata: id=uuid, created_at=ISO, file_type=ext, file_size=len
       ├─ Embedding 重算（bge-m3 经 SiliconFlow API；EMBED_BACKEND=tfidf 时走 TF-IDF）
       ├─ vectors.npy atomic write
       ├─ metadata.json atomic write（KB 指纹随之变更 → 旧 AnswerCache 快照自动失效）
       └─ 返回 {"chunks": N, "strategy": "..."}
```

### 检索链路图（含省 Token 漏斗）

```
            用户问题
                │
        ┌───────┴───────┐
   L0 精确缓存命中?   ├─ 是 → 直返（0 token）★省
        │ 否            │
   L1 语义近似命中?  ├─ 是(hit≥2,cos≥0.92) → 直返（~404 token 省）★省
        │ 否            │
   L2 top1 < 阈值?   ├─ 是(REJECT_THRESHOLD>0) → 拒答不调 LLM ★省
        │ 否            │
        ▼               ▼
   向量召回          BM25+Jieba
   (bge-m3/tfidf)    关键词召回
   top 15            top 15
        │               │
        └───────┬───────┘
                ▼
           RRF 融合 Σ 1/(60+rank)
             top 5
                │
                ▼
       ★ cosine 重排 → top 3
                │
                ▼
      context = top2 × 300字
                │
                ▼
          LLM 生成（glm-4-flash + fabe.txt 接地契约）
```

---

## 📁 文件结构

```
ltc-rag-bot/
├── app.py                  ★ 后端核心 (2073 行, FastAPI 单文件)
├── Dockerfile              CloudBase 容器定义
├── requirements.txt        Python 依赖 (6 个, 总内存 < 50MB)
├── rollback.sh             ★ 一键回滚 (修复版 v2: 无效目标中止 + 自覆盖保护)
├── deploy.sh               部署脚本 (v2: -s <svc> -e <env> --force)
├── cloudbaserc.json        CloudBase 配置 (envParams={}, 不覆盖控制台变量)
├── render.yaml             渲染配置
├── .env.example            环境变量模板（不含真实密钥）
├── .dockerignore / .gitignore
│
├── portal/
│   └── index.html          ★ Portal 前端 (V5 深空指挥台 + 文档全 CRUD)
│
├── configs/
│   └── kb_strategies.json  切片策略配置
│
├── prompts/
│   └── fabe.txt            RAG prompt（接地契约：只准用参考内容 + 主题沾边即拒答）
│
├── scripts/
│   ├── smoke_local.sh      本地冒烟（开发 16/22 项 · 生产 17/17 项）
│   ├── deploy_drill.sh     离线部署/回滚演练（15/15 断言，不联网）
│   └── upgrade_metadata.py 旧 metadata 补字段（幂等 + 备份 + 哈希自校验）
│
├── docs/                   18 份深度文档（架构/坑/缓存/重排/7 份交付文档/巡检报告…）
│
├── design/
│   └── portal-redesign-v3-dual.html  Portal V3 双主题效果图
│
├── kb_docs/                本地 KB 源（显卡与算力需求说明.md 等；线上 KB 在 COS）
│
└── .agents/skills/
    └── rag-knowledge-base-skill/   ★ FDE Skill v1.0.0 (独立可复用)
        ├── SKILL.md                 主入口 · 4 阶段协议
        ├── _references/ (8 份)      pitfall-log + 架构 + 设计
        ├── _commands/ (3 份)        init-baseline / deploy / smoke-test
        ├── _templates/              Dockerfile + requirements
        └── README.md / install.sh / LICENSE
```

---

## 🧩 API 端点（全部可用）

| 方法 | 端点 | 功能 | 鉴权 |
|---|---|---|---|
| GET | `/health` | 健康检查 + 版本 + docs_count + bm25_available + embed_backend | ❌ 免 |
| GET | `/kb` | 知识库概览 (total_chunks + files + by_type) | ✅ |
| GET | `/chunks` | 切片列表 (分页 + 筛选) | ✅ |
| GET | `/chunks/{id}` | 单切片 | ✅ |
| PATCH | `/chunks/{id}` | 编辑切片文本 / source | ✅ |
| DELETE | `/chunks/{id}` | 删除切片 | ✅ |
| GET | `/chunks/options` | 动态筛选选项 (sources / strategies) | ✅ |
| POST | `/query` | RAG 问答 (支持 use_rerank, cache_hit / token 信息返回) | 可选 |
| POST | `/ingest-text` | 粘贴文本入库 | ✅ |
| POST | `/ingest` | 文件上传入库 (multipart) | ✅ |
| GET | `/strategies` | 切片策略列表 | ✅ |
| GET | `/prompts` / `/prompts/{name}` | Prompt 管理 | ✅ |
| PUT | `/prompts/{name}` | 更新 Prompt | ✅ |
| GET | `/embed/info` | 当前 Embedding 后端 / 维度 / 重建状态 | ✅ |
| POST | `/embed/rebuild` | 切换 Embedding 后端并原子重建（tfidf / siliconflow） | ✅ |
| PATCH | `/kb/source/{id}` | 重命名知识源（不重建向量） | ✅ |
| POST | `/kb/source/{id}/replace` | 原子替换知识源（备份→删旧→重切片，失败回滚） | ✅ |
| GET | `/cache` | AnswerCache 快照列表 | ✅ |
| GET | `/cache/stats` | `{total_snapshots, hot_count, max_entries, stale_count}` | ✅ |
| DELETE | `/cache` | 清空缓存（**改阈值/换后端后必清**） | ✅ |
| GET/DELETE | `/stats/token-economy` | M2 省 Token 埋点（按天聚合 / 清零） | ✅ |

---

## 🗄️ 当前数据状态（线上实测 026）

```
COS Bucket: 6c69-lily0625ai-d1gpwc1vw89141edd-1450031534
│
├── metadata.json       215 条切片（已含 created_at/file_type/file_size，09-18 升级）
├── vectors.npy         828KB · bge-m3 向量矩阵 (215 × 1024)
├── vocab.json          36KB · TF-IDF 词表（仅 tfidf 后端用）
└── xiaoliu-identity/   原始 .md 文件 (7 份：6 份原 + 显卡与算力需求说明.md)
```

**切片来源统计**（共 215 条 / 7 文件）：
- 零成本30天FDE案例体系与代码指南_副本.md → 200 chunks（主文件）
- 显卡与算力需求说明.md → 8 chunks（09-19 补，根治 GPU 沾边题编造）
- inline (粘贴) → 3 chunks
- diag.md / test_upload.txt / ok.md / smoke-v3.md → 各 1 chunk

> ✅ `chroma.sqlite3`（196KB 旧 Chromadb 残留）已于 09-18 删除，COS 根目录现 11 个对象。

---

## 🔧 本地启动

```bash
cd ltc-rag-bot
pip install -r requirements.txt
python app.py          # → http://localhost:8080（本地 chroma_data/，COS 未挂载时 fallback）
```
注意：本地路径是 `chroma_data/`（COS 没挂载时自动 fallback）。要看到线上数据 → 从 COS 下载：
```bash
# 经 cloudbase 连接器 queryStorage download，而非 tcb CLI（CLI 未登录）
# metadata.json / vectors.npy / vocab.json → 放到 chroma_data/ 再启动
```

---

## 🚀 部署 / 回滚

> ⚠️ **部署走 cloudbase 连接器 `manageCloudRun.deploy`**（source 构建，**不传 serverConfig** → Read-Merge-Write 保留远程 Cpu/Mem/EnvParams/VolumesConf/OpenAccessTypes，零降级）。`tcb` CLI 当前未登录（clone 502），不要用 CLI 部署。

```bash
cd <项目根>/code
# 部署（连接器，已授权）
# 经 WorkBuddy cloudbase 连接器 manageCloudRun.deploy，source 构建
# 轮询 record list 直到 Status=normal, Traffic=100

# 回滚（脚本 v2，已离线演练 15/15）
bash rollback.sh handoff-20260918 --url <服务URL>
# 实际: 校验目标 tag 本地存在 → git checkout + deploy --force
```

**部署后必须手工确认**（控制台，脚本管不到）：
1. 控制台 → 云托管 → `ltc-rag-bot` → 存储 → 开 **COS 挂载** `/mnt/chroma`
2. 控制台 → 环境变量 → 确认 9 个变量齐全（含 `SOFT_REFUSE_THRESHOLD=0.45`）
3. 改阈值/换 Embedding 后 **`DELETE /cache`** 清快照

---

## 🐛 关键踩坑速查（完整 22 条见 `docs/` 或 skill `_references/pitfall-log.md`）

| # | 坑 | 解法 |
|---|---|---|
| 0.1 | Git 没配 user.email | 部署前 `git config` |
| 0.3 | CloudBase deploy 交互式卡灰度 | `--force 2>&1 <<< $'\n'` |
| 1.3 | AnswerCache 一存就返回 | PROMOTE_THRESHOLD=2，hit_count≥2 才复用 |
| 1.8 | Chromadb 免费版 OOM | 砍了 → 自实现 SimpleVectorStore（COS 持久化） |
| 2.3 | 配色替换漏残留 | grep -c 验收门禁（紫色 hero #7c3aed 为预期） |
| **🔥 P0** | **Portal 字段 h.docs ≠ h.docs_count** | DeployId 017 数据全 0 根因，018 已修 |
| A | COS mount 路径错就全没数据 | Dockerfile 设 CHROMA_PATH=/mnt/chroma |
| **M2** | **改阈值/换后端后不清缓存 → 返回旧答案** | 任何变更后 **`DELETE /cache`** |
| **拒答** | **GPU 类沾边题阈值救不了** | 正解是往 KB 补「显卡需求说明」文档（已做，207→215） |
| **部署** | **tcb CLI 未登录(502)** | 改用 cloudbase 连接器部署，保留远程规格 |

---

## ✅ 026 冒烟 / 演练基线（线上只读实测全绿）

```
✅ /health:       version=3.0, docs_count=215, bm25_available=true, embed_backend=siliconflow(bge-m3)
✅ /kb:           chunks=215, files=7, by_type=['.md','.txt','.inline']
✅ /query "没有显卡能不能跑大模型？": 正确引用 显卡与算力需求说明.md (top1 0.7746)，不再编造
✅ /query "红烧肉怎么做" (库外): 正确拒答（top1 0.3991 < 0.45 软拒答）
✅ /stats/token-economy: 返回真实埋点（queries_total / l0_hits / saved_tokens_est …）
✅ 本地冒烟: 开发 22/22 · 生产 17/17 · 部署演练 15/15
```

---

## 📋 接手后建议的 Next Steps（候选，按价值排序）

| 优先级 | 事项 | 理由 |
|---|---|---|
| P1 | **L1 阈值标定**：`CACHE_SIM_THRESHOLD` 0.92 → 0.88–0.90 | 积累 `/stats/token-economy` 真实分布后降档，换问法命中再省一截 |
| P2 | **CORS 收口**：确认 `/query` 等跨域策略 | 当前未显式限制 |
| P2 | **批量入库限流/大小上限** | 防单次上传打爆 2GB 内存 |
| P3 | **`docs/13项冒烟验收.md` 同步新增端点** | `/embed/*`、`/stats/token-economy`、`/kb/source/*` |
| ✅ 已完成 | Upgrade 旧 metadata / 清 chroma.sqlite3 / Embedding 升级 / Portal V5 / M2 / 拒答双层 / GPU KB | 见 Done #21–#33 |

---

## 🔐 API Keys

```
API_KEY=<API_KEY>           # Portal / API 鉴权
ZHIPUAI_API_KEY=<ZHIPUAI_API_KEY>
ZHIPUAI_MODEL=glm-4-flash
FEISHU_APP_ID=cli_aab2169e42badbea
FEISHU_APP_SECRET=<FEISHU_APP_SECRET>
EMBED_BACKEND=siliconflow
EMBED_MODEL=BAAI/bge-m3
EMBED_API_URL=https://api.siliconflow.cn/v1/embeddings
SOFT_REFUSE_THRESHOLD=0.45
CHROMA_PATH=/mnt/chroma
```
> ⚠️ 以上密钥只存在 CloudBase 控制台与本地 `code/.env`（已 gitignore，未入库、未进交付包）。**请勿提交公开仓库**。

---

## 🧭 FDE Skill

完整可复用 Skill 已随项目创建：
- 路径: `.agents/skills/rag-knowledge-base-skill/`
- 独立安装: `git clone` 后运行 `./install.sh`（一键装到 skills 目录）
- 包含: 4 阶段协议、22 条坑、8 份参考文档、3 个可执行脚本、代码模板
- 下次类似项目直接 clone 安装即可，跳过所有坑
