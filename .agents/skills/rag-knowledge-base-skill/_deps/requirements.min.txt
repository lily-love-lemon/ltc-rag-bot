# CloudBase 免费版兼容（总内存 < 50MB）
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
scikit-learn>=1.3.0    # TF-IDF (numpy 自带)
jieba>=0.42.1          # 中文分词
python-dotenv>=1.0.0
requests>=2.31.0       # 飞书 API 调用
# NO chromadb, langchain, sentence-transformers  —— 免费版内存装不下
